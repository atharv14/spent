import type { NextConfig } from "next";
const nextConfig = {
  /* config options here */
  output: "export",
  basePath: "/nextjs-github-pages",
  images: {
    unoptimized: true,
  },
};
export default nextConfig;
